spring:
  datasource:
    url: jdbc:sqlserver://localhost;instanceName=SQLEXPRESS;databaseName=Practice;encrypt=false;trustServerCertificate=true
    username: money
    password: money0816
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.SQLServerDialect
mqtt:
  url: tcp://localhost:1883
  client-id: springboot-consumer
  username: guest
  password: guest
  topic: test/topic
  enabled: false
server:
  port: 8085
  error:
    include-message: always